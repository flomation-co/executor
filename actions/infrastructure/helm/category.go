// Category metadata for the Infrastructure ▸ Helm sub-group. Harvested by
// cmd/manifest/manifest.go; mirrored in the api's subCategoryMetadata map
// (api/internal/http/action.go), so keep the two in sync.
package helm

const (
	CategoryName        = "Helm"
	CategoryIcon        = "helm"
	CategoryDescription = "Install, upgrade, roll back and inspect Helm releases on a Kubernetes cluster"
)
