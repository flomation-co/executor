// Category metadata for the Infrastructure ▸ Kubernetes sub-group. Harvested by
// cmd/manifest/manifest.go; mirrored in the api's subCategoryMetadata map
// (api/internal/http/action.go), so keep the two in sync.
package kubernetes

const (
	CategoryName        = "Kubernetes"
	CategoryIcon        = "kubernetes"
	CategoryDescription = "Operate a Kubernetes cluster — restart and scale deployments, read pod logs, run jobs, manage config, and drain nodes"
)
