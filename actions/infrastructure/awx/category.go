// Category metadata for the Infrastructure ▸ AAP / AWX sub-group. Harvested by
// cmd/manifest/manifest.go; mirrored in the api's subCategoryMetadata map
// (api/internal/http/action.go), so keep the two in sync.
package awx

const (
	CategoryName        = "AAP / AWX"
	CategoryIcon        = "ansible"
	CategoryDescription = "Ansible Automation Platform / AWX — launch existing job templates and workflows, watch jobs to completion, and manage inventories, hosts, projects, credentials and schedules. This node talks to the AWX/AAP controller's API; it does not run playbooks itself."
)
