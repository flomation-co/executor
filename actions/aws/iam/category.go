package iam

const (
	CategoryName        = "IAM"
	CategoryIcon        = "shield-halved"
	CategoryDescription = "Identity and Access Management — users, groups, roles, policies, access keys and instance profiles"
)
