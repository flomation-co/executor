package secretsmanager

const (
	CategoryName        = "Secrets Manager"
	CategoryIcon        = "lock"
	CategoryDescription = "Store, retrieve and rotate secrets — secret values, versions, rotation and resource policies"
)
