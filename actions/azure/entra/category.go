// Category metadata for the Azure ▸ Entra ID sub-group. Harvested by
// cmd/manifest/manifest.go; mirrored in the api's subCategoryMetadata map
// (api/internal/http/action.go), so keep the two byte-identical.
package entra

const (
	CategoryName        = "Entra ID"
	CategoryIcon        = "id-badge"
	CategoryDescription = "Microsoft Entra ID (Azure AD) — users, groups, membership, licences, and guest invites"
)
