// Category metadata for the DevOps ▸ Jenkins sub-group. Harvested by
// cmd/manifest/manifest.go; mirrored in the api's subCategoryMetadata map
// (api/internal/http/action.go), so keep the two in sync.
package jenkins

const (
	CategoryName        = "Jenkins"
	CategoryIcon        = "jenkins"
	CategoryDescription = "Trigger and manage Jenkins jobs and builds, and control the Jenkins server"
)
