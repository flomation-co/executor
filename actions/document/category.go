package document

// Document actions generate and populate documents: filling PDF forms today,
// with templating and PDF/chart export to follow. Category metadata is read by
// the manifest generator (AST) and applied to every action in this tree.
const (
	CategoryName        = "Document"
	CategoryIcon        = "file-lines"
	CategoryDescription = "Generate and populate documents (fill PDF forms, templates, export)"
)
